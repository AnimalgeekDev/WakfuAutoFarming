vamos a hacer una reforma a este proyecto, inicialmente lo use para validar los modelos yolov8 y yolo26, pero ahora no me sirve, necesito que refactorices todo el proyecto, quiero lograr lo siguiente:

* una aplicación en python con interfaz grafica simple
* la ventana debe tener un tamaño 450 de ancho y 500 de alto que se posicina en la esquina inferior de la pantalla del pc, la ventana esta divida en 4 secciones ubicadas de forma vertical, la priemra area debe tener un checkbox con el label "Realizar acciones", un checkbox con el label "Mostrar centros", la segunda area un texto que aparezca debajo de la primera area y solo aparece cuando el checkbox de "Realizar acciones" esta activo, el texto debe decir "presione Q para detener acciones", la tercera seccion de la ventana debe tener una imagen que va cambiar con el tiempo que debe tener los 450 de ancho y 250 de alto fijos dentro de la ventana, si la imagen es null se debe mosrtar un texto que diga NO IMAGE en el centro del espacio para la imagen y la cuarta seccion un boton que diga "Salir"
* la aplicacion en python debe tomar un screenshot de un proceso (wakfu.exe) para obtener una imagen de la ventana de ese proceso, luego, debe pasar esa imagen por un modelo yolo cargado previamente, y el resultado de ese modelo, la imagen de salida con los cajones de la detección del objeto debe ser mostrado en el espacio para imagen de la ventana actual, para poder ver los cajones en "tiempo real" (toma en cuenta que no es necesario ejeuctar 25 fotogramas por segundo en la ventana, debe cambiar la iamgen a la velocidad que el proceso termine)
* si esta marcado el checkbox "Mostrar centros", la imagen de salida debe ubicar en el centro de la caja un punto de color aleatorio, para esto, es mejor descartar la imagen obtenida por el modelo yolo y solo usar los cajones para obtener los centros y luego sobreponer el punto de color sobre la imagen original capturada
* deja preparado un flujo para la opción "Realizar acciones" dado que esto lo haremos mas adelante, pero esta seccion valida si el check esta activado, luego determina si el usaurio preciona la tecla Q, de ser así frena este metodo y posterior cambia el checkbox a falso, en caso de ser verdadero la aplicación debe buscar la caja mas cerca y solo la primera mas cercana al centro de la imagen capturada del proceso (wakfu.exe), luego obtener el centro del cajon mas cercano (los centros deben ser calculados previamente tanto para la imagen del preview de la ventana como para este paso) debe simular un click derecho en el centro de esa caja, luego debe esperar un segundo (1000 milisegundos), luego debe levantar un flag de espera a encontrar acciones, esto porque el proceso debe tomar de nuevo una iamgen, pasar la imagen por el modelo yolo, obtener los resultados, cargar la nueva preview en la ventana y, si este flag esta activo al momento de proesar la iamgen, debe buscar en las cajas encontradas si existe una con nombre get_seed o get_item y simular un click izquierdo en el centro de la caja, si existen las dos cajas con los nombres indicados se debe priorizar get_seed para el click izquierdo, luego de este click izquierdo en las acciones se debe pausar este proceso unos 4 segundos (4000 milisegundos)
* el proceso de validar en la imagen capturada y procesada por yolo si existe alguna caja llamada "capitan_miau", si existe esta caja debe generar una notificación al sistema operativo windows indicando la situación de "Capitan miau detectado, se necesita intervención", si el sistema detecta esta caja tambien debe pasar el checkbox de "Ejecutar acciones" a falso
* la aplicacion debe responder a este flujo, interfaz grafica -> tomar screenshot -> pasar la imagen por el modelo YOLO -> calcular los centros de las cajas obtenidas por el modelo yolo -> determinar si esta check "Mostrar centros" para actualizar la iamgen de la interfaz grafica con los puntos o con las cajas -> pasar los datos obtenidos por yolo a un metodo de procesar imagen si el checkbox "Realizar acciones" esta marcado, si no esta marcado el programa no hace nada mas -> si esta marcado el checkbox se debe -> determinar si esta el capitan_miau en las cajas para marcar el checkbox "Realizar acciones" en falso, generar la notificación y no realizar mas acciones -> si no esta capitan miau se debe determinar si el flag de esperar acciones esta activo -> si lo esta se debe buscar la caja de acciones, si no hay una caja de acciones se debe esperar hasta que yolo detecte una con cada imagen procesada, si no se detectan acciones en 5 segunso (5000 milisegundos) se debe pasar el flag a falso y enviar una notifiación de windows al usuario indicando "Sin acciones?" -> si se detecta get_seed y get_item se debe simular un el ckick izquierdo en get_seed, si solo esta get_item, se simula en get_item -> luego de este click el flujo de procesamiento de la iamgen debe detenerse por 5 segundos (5000 milisegundos) para esperar el comportamiento en la pc y luego volver a procesar imagenes con el flujo mencionado
* la aplicación no usará argumentos de consola (como --argumento) pero si un archivo .env el cual debe tener todos los posibles elementos de configuración como los tiempos de espera, ubicación del modelo (debe ser una ruta relativa), texto usados en las notificaciones, textos usados en la interfaz grafica
* busca fallos en el flujo o posibles casos no contemplados en el flujo y la descripción anterior, mencionalos antes de inicar todo el refactor para dejar muy claro todo el proceso
* debes aplicar clean code, metodos con nombres claros y especificos, contempla posibles errores y genera un raisen excepion usando el nombre del metodo y el error, la app debe tener solo un try catch que controle los errores que proviene de los diversos metodos
* refactoriza todo el proyecto eliminando archivos no usados en este refacto
* valida que todo lo necesario este registrado en requirements.txt
* debes documentar cada metodo del proyecto generado
* los metodos deben estar tipeados y no tener valores por defecto en sus parametros
* intenta separar la logica lo mas posible para aplicar clean code
* los scrips de data_aumentation y run_yolo debe ser eliminados
* debes ajustar train_yolo para ser ejeuctado como un script aparte de la interfaz grafica, este script debe ser completamente indendiente, y debe tener si propio .env dentro de la carpeta scripts, se debe ajustar para obtener desde .env el modelo a usar, la cantidad de epoch (que puede ser un array de valores), el device y el proyect_name
* el script de train_yolo debe iterar sobre las posibles epoch y realizar el entrenamiento similar a como se hace actualmente, ajustando el name usando el mnombre del modelo y las epoch
* simplifica el script para no usar tantos metodos, con solo usar __main__ creo que se puede resolver todo el script de train_yolo